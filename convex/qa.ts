/**
 * @deprecated Use `shared/qa.ts` for the canonical rubric definitions and
 * prompt-building logic, or `convex/rubrics.ts` for the default criteria
 * constant. This file is kept only for backwards compatibility.
 */
export {
  RUBRIC_CRITERIA,
  PASSING_SCORE,
  buildQaSystemPrompt,
} from '../shared/qa';
